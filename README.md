apt-X100 Library

Installation Instructions:
* Build from source by running ```make```
* Install via ```make install```, provide sudo password if prompted

When linking, use the gcc command line option ```-laptx100```

Documentation:
TODO